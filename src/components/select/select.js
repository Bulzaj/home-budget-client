import Typography from "../../typography/typography";

const Select = (props) => {
  return (
    <div className="select">
      <Typography.Title5>{props.title}</Typography.Title5>
    </div>
  );
};

export default Select;
